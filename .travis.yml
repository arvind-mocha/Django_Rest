# this file tell travis what to do every time when the changes are pushed to github

language: python
python:
  - "3.6"

service:
  - docker

before_script: pip install docker-compose

script:
  - docker-compose run app sh -c "py manage.py test && flake8"
# flake8 is the lintin tool
